# Local overrides for this CLI experiment (auto-discovered under this directory).

line-length = 120
target-version = "py310"

[lint]
select = ["E", "F", "I", "UP", "W"]
ignore = [
  "E402", # sys.path bootstrap before local imports
]

[lint.per-file-ignores]
"test_*.py" = ["S101"]
