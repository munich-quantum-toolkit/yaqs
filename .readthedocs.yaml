version: 2

formats:
  - htmlzip

sphinx:
  configuration: docs/conf.py

build:
  os: ubuntu-26.04
  tools:
    python: "3.14"

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs
